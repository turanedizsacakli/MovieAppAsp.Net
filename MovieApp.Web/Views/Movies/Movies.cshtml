@model List<Movie>

@section menu {
    @await Html.PartialAsync("_menu")
}

    <h1>Film Listesi</h1>

    <div id="filmler">

        @foreach (var movie in Model)
        {
            @await Html.PartialAsync("_movie", movie)
        }
    </div>

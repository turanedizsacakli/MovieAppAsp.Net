@model List<Genre>


<div class="list-group">
    @foreach (var movie in Model)
        {

        <a href="#" class="list-group-item list-group-item-action">@movie.Name</a>

        }

</div>

@* <button type="button" class="list-group-item list-group-item-action active mt-5" aria-current="true">
    @movie.Name
</button> *@

